{
    "configurations": [
        {
            "type": "chrome",
            "name": "localhost:8080/home",
            "request": "launch",
            "url": "localhost:8080/home"
        }
    ]
}